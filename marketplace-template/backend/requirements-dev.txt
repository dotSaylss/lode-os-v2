# ── Test / development dependencies ──────────────────────────────────────────
# Install alongside the core deps to run the test suite:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# These are NOT needed to run the server — only to run `pytest`.
pytest
httpx            # required by FastAPI's TestClient
